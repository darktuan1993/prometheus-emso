apiVersion: v1
kind: Service
metadata:
  name: {{ include "emso-prometheus-helm.fullname" . }}
  labels:
    {{- include "emso-prometheus-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "emso-prometheus-helm.selectorLabels" . | nindent 4 }}
