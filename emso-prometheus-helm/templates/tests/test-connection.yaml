apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "emso-prometheus-helm.fullname" . }}-test-connection"
  labels:
    {{- include "emso-prometheus-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "emso-prometheus-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
